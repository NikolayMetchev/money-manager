# AGENTS.md

**Read [`CLAUDE.md`](CLAUDE.md).** It is the single source of guidance for this repository —
build commands, module layout, and the architectural rules every agent must follow.

This file exists only because Codex looks for this filename. Deliberately keep it a pointer: a
second copy of the rules drifts, and then half the time an agent reads the stale one.
