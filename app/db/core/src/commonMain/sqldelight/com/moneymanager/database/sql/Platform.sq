-- Platform: Lookup table for platform types (JVM, ANDROID)
CREATE TABLE Platform (
    id INTEGER PRIMARY KEY NOT NULL,
    name TEXT NOT NULL UNIQUE
);

insert:
INSERT OR IGNORE INTO Platform (id, name) VALUES (?, ?);
