-- os_name: Lookup table for operating system names
CREATE TABLE os_name (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    name TEXT NOT NULL UNIQUE
);

selectByName:
SELECT id FROM os_name WHERE name = ?;

insertOrIgnore:
INSERT OR IGNORE INTO os_name (name) VALUES (?);
