-- AuditType lookup table for audit operations
CREATE TABLE AuditType (
    id INTEGER PRIMARY KEY,
    name TEXT NOT NULL UNIQUE
);

-- Insert audit type (used during seeding)
insert:
INSERT INTO AuditType (id, name) VALUES (?, ?);
