CREATE TABLE attribute_type (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    name TEXT NOT NULL UNIQUE
);

selectAll:
SELECT * FROM attribute_type ORDER BY name;

selectById:
SELECT * FROM attribute_type WHERE id = ?;

selectByName:
SELECT * FROM attribute_type WHERE name = ?;

insert:
INSERT INTO attribute_type (name) VALUES (?);
